export default function makePostOrder({
    ordersDb,
    orderItemsDb,
    getPromoCodeDetails
}) {
    return async function postOrder({ body, headers, user }) {

        // Validate parameters...
        // Create order...
        // Create order items...

        return order;
    };
}